<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buat Surat Baru - SIPRESISI TIK</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-[#EAEABA] flex h-screen overflow-hidden">

    <aside class="w-64 bg-[#1a1a1a] text-white flex flex-col p-6">
        <img src="/image/logo_tik.png" class="w-32 mx-auto mb-10" alt="Logo">
        
        <nav class="space-y-4 flex-1">
            <a href="/staff/dashboard" class="flex items-center p-3 hover:bg-gray-800 rounded-xl transition text-sm">
                <i class="fas fa-th-large mr-3"></i> Dashboard
            </a>
            <a href="/staff/buat-surat" class="flex items-center p-3 bg-white text-black rounded-xl font-bold text-sm shadow-lg">
                <i class="fas fa-file-signature mr-3"></i> Buat surat baru
            </a>
            <a href="/staff/daftar-surat" class="flex items-center p-3 hover:bg-gray-800 rounded-xl transition text-sm">
                <i class="fas fa-list mr-3"></i> Daftar surat saya
            </a>
            <a href="/staff/pencarian" class="flex items-center p-3 hover:bg-gray-800 rounded-xl transition text-sm">
                <i class="fas fa-search mr-3"></i> Pencarian surat
            </a>
        </nav>

        <div class="mt-auto pt-6 border-t border-gray-800">
            <a href="/auth/logout" class="flex items-center p-3 text-red-400 hover:bg-red-500 hover:text-white rounded-xl transition font-bold text-sm">
                <i class="fas fa-sign-out-alt mr-3"></i> Logout
            </a>
        </div>
    </aside>

    <main class="flex-1 p-8 overflow-y-auto">
        <header class="sticky top-0 z-30 bg-[#EAEABA] px-8 py-5 -mx-8 border-b border-black/10 shadow-[0_2px_0_rgba(0,0,0,0.12)]">
            <div class="flex items-center">
                <div class="w-14 h-14 bg-blue-600 rounded-full flex items-center justify-center text-white font-bold text-xl mr-4 shadow-md">
                    <%= user ? user.charAt(0).toUpperCase() : 'S' %>
                </div>

                <div class="flex flex-col justify-center">
                    <span class="font-bold text-base text-gray-800 leading-tight">
                        <%= user %>
                    </span>
                    <span class="text-sm text-gray-500 leading-tight font-medium">
                        Staff
                    </span>
                </div>
            </div>
        </header>

        <div class="mt-10 mb-8">
            <h3 class="text-2xl font-bold text-gray-800">Buat Surat Baru</h3>
            <p class="text-gray-500 text-sm mt-1">Pilih Jenis Surat dan Template</p>
        </div>

        <div class="bg-white rounded-3xl p-8 shadow-sm border border-gray-50 flex items-center justify-between mb-10">
            <div class="flex items-center gap-4 flex-1">
                <div class="w-12 h-12 bg-blue-600 text-white rounded-2xl flex items-center justify-center font-bold shadow-lg shadow-blue-100">1</div>
                <div>
                    <p class="text-sm font-bold text-gray-800">Pilih Jenis Surat</p>
                    <p class="text-[11px] text-gray-400">Tentukan Jenis Surat yang Akan dibuat</p>
                </div>
            </div>
            <i class="fas fa-chevron-right text-gray-200 mx-6"></i>
            <div class="flex items-center gap-4 flex-1 opacity-40">
                <div class="w-12 h-12 bg-gray-100 text-gray-400 rounded-2xl flex items-center justify-center font-bold">2</div>
                <div>
                    <p class="text-sm font-bold text-gray-800">Pilih Template</p>
                    <p class="text-[11px] text-gray-400">Pilih Template yang Sesuai</p>
                </div>
            </div>
            <i class="fas fa-chevron-right text-gray-200 mx-6"></i>
            <div class="flex items-center gap-4 flex-1 opacity-40">
                <div class="w-12 h-12 bg-gray-100 text-gray-400 rounded-2xl flex items-center justify-center font-bold">3</div>
                <div>
                    <p class="text-sm font-bold text-gray-800">Isi Konten</p>
                    <p class="text-[11px] text-gray-400">Lengkapi Isi Surat</p>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-[2.5rem] p-10 shadow-sm border border-gray-100 min-h-[450px]">
            <h4 class="text-xl font-bold text-gray-800 mb-8 border-b border-gray-50 pb-6">Pilih Jenis Surat</h4>
            
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <% if (templates && templates.length > 0) { %>
                    <% templates.forEach(type => { %>
                    <a href="/staff/buat-surat/template/<%= type.id %>" 
                       class="group border border-gray-100 p-8 rounded-3xl hover:border-blue-500 hover:shadow-xl hover:shadow-blue-50/50 transition-all bg-gray-50/20">
                        <div class="flex items-center gap-5 mb-8">
                            <div class="w-14 h-14 bg-blue-50 text-blue-600 rounded-2xl flex items-center justify-center font-bold text-sm uppercase group-hover:bg-blue-600 group-hover:text-white transition-all shadow-sm">
                                <%= type.name.split(' ').map(w => w[0]).join('').substring(0,2) %>
                            </div>
                            <h5 class="font-bold text-gray-800 text-base leading-tight"><%= type.name %></h5>
                        </div>
                        <p class="text-xs text-gray-400 leading-relaxed font-medium">
                            Surat Resmi Berisi <span class="text-blue-500/80"><%= type.name %></span> Organisasi Bid TIK Polda
                        </p>
                        <div class="mt-6 flex justify-end">
                            <span class="text-[10px] font-bold text-blue-600 bg-blue-50 px-3 py-1 rounded-full opacity-0 group-hover:opacity-100 transition-opacity">
                                Pilih Template <i class="fas fa-arrow-right ml-1"></i>
                            </span>
                        </div>
                    </a>
                    <% }) %>
                <% } else { %>
                    <div class="col-span-3 text-center py-20 bg-gray-50/50 rounded-3xl border border-dashed border-gray-200">
                        <div class="w-20 h-20 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-6">
                            <i class="fas fa-folder-open text-3xl text-gray-300"></i>
                        </div>
                        <p class="text-gray-400 font-medium italic">Belum ada kategori surat yang memiliki template resmi.</p>
                        <p class="text-xs text-gray-400 mt-2">Silakan hubungi Administrator untuk unggah template (.docx)</p>
                    </div>
                <% } %>
            </div>
        </div>
    </main>

</body>
</html>