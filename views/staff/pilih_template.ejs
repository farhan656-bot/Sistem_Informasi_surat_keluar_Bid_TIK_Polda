<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pilih Template - SIPRESISI TIK</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-[#EAEABA] flex h-screen overflow-hidden">

    <aside class="w-64 bg-[#1a1a1a] text-white flex flex-col p-6">
        <img src="/image/logo_tik.png" class="w-32 mx-auto mb-10" alt="Logo">
        <nav class="space-y-4 flex-1">
            <a href="/staff/dashboard" class="flex items-center p-3 hover:bg-gray-800 rounded-xl transition text-sm">
                <i class="fas fa-th-large mr-3"></i> Dashboard
            </a>
            <a href="/staff/buat-surat" class="flex items-center p-3 bg-white text-black rounded-xl font-bold text-sm shadow-lg">
                <i class="fas fa-file-signature mr-3"></i> Buat surat baru
            </a>
            <a href="/staff/daftar-surat" class="flex items-center p-3 hover:bg-gray-800 rounded-xl transition text-sm">
                <i class="fas fa-list mr-3"></i> Daftar surat saya
            </a>
            <a href="/staff/pencarian" class="flex items-center p-3 hover:bg-gray-800 rounded-xl transition text-sm">
                <i class="fas fa-search mr-3"></i> Pencarian surat
            </a>
        </nav>
        <div class="mt-auto pt-6 border-t border-gray-800">
            <a href="/auth/logout" class="flex items-center p-3 text-red-400 hover:bg-red-500 hover:text-white rounded-xl transition font-bold text-sm">
                <i class="fas fa-sign-out-alt mr-3"></i> Logout
            </a>
        </div>
    </aside>

    <main class="flex-1 p-8 overflow-y-auto">
        <header class="sticky top-0 z-30 bg-[#EAEABA] px-8 py-5 -mx-8 border-b border-black/10 shadow-[0_2px_0_rgba(0,0,0,0.12)]">
            <div class="flex items-center">
                <div class="w-14 h-14 bg-blue-600 rounded-full flex items-center justify-center text-white font-bold text-xl mr-4 shadow-md">
                    <%= user ? user.charAt(0).toUpperCase() : 'S' %>
                </div>
                <div class="flex flex-col justify-center">
                    <span class="font-bold text-base text-gray-800 leading-tight"><%= user %></span>
                    <span class="text-sm text-gray-500 leading-tight font-medium">Staff</span>
                </div>
            </div>
        </header>

        <div class="mt-10 mb-8">
            <h3 class="text-2xl font-bold text-gray-800">Buat Surat Baru</h3>
            <p class="text-gray-500 text-sm mt-1">Pilih jenis surat dan template untuk memulai</p>
        </div>

        <div class="bg-white rounded-3xl p-8 shadow-sm border border-gray-50 flex items-center justify-between mb-10">
            <div class="flex items-center gap-4 flex-1 opacity-40">
                <div class="w-12 h-12 bg-gray-100 text-gray-400 rounded-2xl flex items-center justify-center font-bold">1</div>
                <div><p class="text-sm font-bold text-gray-800">Pilih Jenis Surat</p></div>
            </div>
            <i class="fas fa-chevron-right text-gray-200 mx-6"></i>
            <div class="flex items-center gap-4 flex-1">
                <div class="w-12 h-12 bg-blue-600 text-white rounded-2xl flex items-center justify-center font-bold shadow-lg shadow-blue-100">2</div>
                <div>
                    <p class="text-sm font-bold text-gray-800">Pilih Template</p>
                    <p class="text-[11px] text-gray-400">Pilih template yang sesuai</p>
                </div>
            </div>
            <i class="fas fa-chevron-right text-gray-200 mx-6"></i>
            <div class="flex items-center gap-4 flex-1 opacity-40">
                <div class="w-12 h-12 bg-gray-100 text-gray-400 rounded-2xl flex items-center justify-center font-bold">3</div>
                <div><p class="text-sm font-bold text-gray-800">Isi Konten</p></div>
            </div>
        </div>

        <div class="bg-white rounded-[2.5rem] p-10 shadow-sm border border-gray-100 min-h-[450px] flex flex-col">
            <div class="flex justify-between items-center mb-10 border-b border-gray-50 pb-6">
                <h4 class="text-xl font-bold text-gray-800">Pilih Template</h4>
                <a href="/staff/buat-surat" class="text-blue-600 font-bold text-xs hover:underline flex items-center">
                    <i class="fas fa-arrow-left mr-2"></i> Ubah Jenis Surat
                </a>
            </div>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8 flex-1">
                <% if (templates && templates.length > 0) { %>
                    <% templates.forEach(temp => { %>
                    <a href="/staff/download-template/<%= temp.id %>" 
                       class="flex items-center gap-5 border border-gray-100 p-8 rounded-3xl hover:border-blue-500 hover:bg-blue-50/10 transition-all group">
                        <div class="w-14 h-14 bg-blue-50 text-blue-600 rounded-2xl flex items-center justify-center group-hover:bg-blue-600 group-hover:text-white transition-all shadow-sm">
                            <i class="fas fa-file-word text-xl"></i>
                        </div>
                        <div>
                            <h5 class="font-bold text-gray-800 text-base"><%= temp.template_name %></h5>
                            <p class="text-[11px] text-gray-400 font-medium">Klik untuk mengunduh template ini</p>
                        </div>
                    </a>
                    <% }) %>
                <% } %>

                <div class="flex items-center gap-5 border-2 border-dashed border-gray-100 p-8 rounded-3xl opacity-50 cursor-not-allowed bg-gray-50/50">
                    <div class="w-14 h-14 bg-gray-100 text-gray-400 rounded-2xl flex items-center justify-center">
                        <i class="fas fa-file-pen text-xl"></i>
                    </div>
                    <div>
                        <h5 class="font-bold text-gray-800 text-base">Tanpa Template</h5>
                        <p class="text-[11px] text-gray-400 font-medium">Fitur ini belum tersedia</p>
                    </div>
                </div>
            </div>

            <div class="mt-10 pt-6 border-t border-gray-50 flex justify-end">
                <a href="/staff/buat-surat/isi-konten/<%= typeId %>" 
                   class="bg-blue-600 hover:bg-blue-700 text-white px-10 py-4 rounded-2xl font-bold text-sm transition flex items-center shadow-lg shadow-blue-100 group">
                    Selanjutnya
                    <i class="fas fa-chevron-right ml-3 text-[10px] transition-transform group-hover:translate-x-1"></i>
                </a>
            </div>
        </div>
    </main>
</body>
</html>